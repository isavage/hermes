# ─── Hermes data — NEVER commit ─────────────────────────────────────────
# Hermes state now lives in the named Docker volume `hermes-data`, NOT in a
# repo folder. This entry is kept as a defensive guard in case a local
# .hermes/ directory is ever created (e.g. for local dev); nothing in it is
# tracked or deployed.
.hermes/

# Local env / secrets (never commit)
.env
.env.*
!.env.example

# OS / editor junk
.DS_Store
